{
  "name": "local-data-sharing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.971.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
